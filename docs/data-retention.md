# Data retention

Default production policy should be configurable per organization. Temporary parsing files should be deleted automatically after processing. Users must be able to delete projects, source files, generated documents, and accounts, with permanent deletion jobs recorded in an audit log.
